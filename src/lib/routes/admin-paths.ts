/** Canonical platform admin URLs. */
export const ADMIN_ROUTES = {
  root: "/admin",
  users: "/admin/users",
  insuranceTemplates: "/admin/insurance-templates",
  aiTraceEvents: "/admin/ai-trace-events",
} as const;
